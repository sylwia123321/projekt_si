{% include 'default/_form.html.twig' with {
    page_title: 'title.avatar_edit'|trans({'%id%': avatar.id|default('')}),
    form: form,
    submit_label: 'action.edit'|trans,
    back_to_list_path: 'avatar_index',
} %}