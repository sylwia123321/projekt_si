{% extends 'base.html.twig' %}

{% block title %}{{ 'action.register'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'action.register'|trans }}</h1>
    {{ form_start(registrationForm) }}
    {{ form_widget(registrationForm) }}
    {{ form_end(registrationForm) }}
{% endblock %}
